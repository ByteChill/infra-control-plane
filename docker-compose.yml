services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_DB: infra_control_plane
      POSTGRES_USER: infra
      POSTGRES_PASSWORD: infra
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
